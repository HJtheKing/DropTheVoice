server:
  port: 8080

spring:
  application:
    name: dropthevoice

  datasource:
    url: jdbc:mysql://localhost:3307/dropthevoice
    username: ssafy
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

  #data:
    #mongodb:
      #uri: mongodb://root:password@localhost:27017/chatdatabase

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    root: info